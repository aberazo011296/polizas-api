[pytest]
# Cobertura: medir app/ y FALLAR si baja del 80% (puerta de calidad).
# Correr: venv/bin/python -m pytest
addopts = --cov=app --cov-report=term-missing --cov-fail-under=80
asyncio_mode = auto
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
